import 'package:flutter/material.dart';

class DarkThemeColor {
  const DarkThemeColor._();

  static const primaryDark = Color(0xFF35012c);
  static const primaryLight = Color(0xFF290025);
}

class LightThemeColor {
  LightThemeColor._();

  static const primaryDark = Color(0xFFfaedcd);
  static const primaryLight = Color(0xFFfaedcd);
  static const accent = Color(0xFFFD8629);
  static const yellow = Color(0xFFFFBA49);
}
